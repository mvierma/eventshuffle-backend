services:
  app:
    image: 'eventshuffle-backend:latest'
    build:
      context: .
    container_name: app
    depends_on:
      - db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=newpassword
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/eventshuffle-backend
      - SPRING_DATASOURCE_USERNAME=postgres 
      - SPRING_DATASOURCE_PASSWORD=newpassword
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    ports:
      - "8080:8080"

  db:
    image: postgres
    container_name: db
    environment:
      - POSTGRES_USER=postgres 
      - POSTGRES_PASSWORD=newpassword 
      - POSTGRES_DB=eventshuffle-backend